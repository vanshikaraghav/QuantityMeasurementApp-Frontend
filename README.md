# QuantityMeasurementApp-Frontend



## Run the Angular app

1. Open a terminal in `quantity-measurement-app`
2. Run `npm install` if needed
3. Run `npm start` or `npx ng serve`
4. Open `http://localhost:4200`

